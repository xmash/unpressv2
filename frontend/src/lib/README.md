# lib

Client-side helpers:

- recover-worker bootstrap + typed messaging,
- blob / download utilities,
- pricing and format maps (`$19/site`, `+$59` store),
- API client for the backend (`NEXT_PUBLIC_API_URL`) — checkout & entitlement.
